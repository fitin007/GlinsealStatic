spec:
  name: sample-dockerfile-static
  static_sites:
    - name: web
      git:
        branch: main
        repo_clone_url: https://github.com/fitin007/GlinsealStatic.git
      dockerfile_path: Dockerfile.static
      output_dir: /app/public
